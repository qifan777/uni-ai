export io.qifan.server.menu.entity.Menu
input MenuInput {
    #allScalars(Menu)
    id?
}

specification MenuSpec {
    #allScalars
    like/i(name)
    like/i(path)
    like/i(id)
    ge(createdTime)
    le(createdTime)
    ge(editedTime)
    le(editedTime)
}